/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['logos-download.com', 'lens.infura-ipfs.io', 'ik.imagekit.io', 'cdn.stamp.fyi']
    }
}

module.exports = nextConfig
